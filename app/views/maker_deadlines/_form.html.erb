<%= simple_form_for @maker, :html => { :class => 'form-horizontal' } do |f| %>

  <%= f.input :メーカー%>
  期限
  <br>
  <%= f.input_field :期限, collection: 0..10%> 年間
  <br>
  <br>
  <%= f.button :submit, :class => 'btn-primary' %>
  <%#= link_to t('.cancel', :default => t("helpers.links.cancel")),
                maker_deadlines_path, :class => 'btn btn-default' %>
  <%= link_to "Cancel" , :back %>
<% end %>
